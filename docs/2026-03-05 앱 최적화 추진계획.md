# 2026-03-05 앱 최적화 추진계획

**작성일**: 2026년 3월 5일
**프로젝트**: 내손안에 가이드 (누비)
**환경**: Replit 풀스택, Expo WebView, 구글 플레이 내부 테스트

---

## 누비 앱 — 전체 구조도 (2026-03-09 전수 검사)

> P1-4 ALL-SPA 전환 전 현재 앱 구조 뼈대. 누락 방지용.

```
📱 누비 앱 (WebView SPA)
│
├─── 🔵 인앱 SPA 페이지 (7개) ─── index.html 내 showPage()
│    │
│    ├─ 1. landingPage — 언어 선택
│    │     ├─ 로고 + "내 손안에 가이드" 타이틀
│    │     ├─ 언어 리스트 7개국어 (KR, US, FR...)
│    │     └─ 선택 시 → featuresPage (1터치, 구글번역스페너 준비)
│    │
│    ├─ 2. featuresPage — 기능 설명
│    │     ├─ 기능 카드 3개
│    │     │   ├─ 좌: 저장 + "자세히 보기" → 인포그래픽 모달 ⚠️ SPA 닫기 적용
│    │     │   ├─ 중: 카메라 + "영상 보기" → 동영상 모달 ⚠️ SPA 닫기 적용
│    │     │   └─ 우: 마이크 + "말하기만 하세요"
│    │     ├─ 사용 단계 1-2-3 안내
│    │     └─ "바로 시작하기" → mainPage
│    │
│    ├─ 3. mainPage — 카메라 라이브뷰 + 입력
│    │     ├─ 전면 카메라 라이브뷰 (전체 배경)
│    │     ├─ 하단 버튼 4개: 촬영 / 음성(중앙) / 업로드 / 보관함
│    │     └─ 토스트: 크레딧 부족, 카메라 실패, 음성 미지원/실패, 로그인 안내
│    │
│    ├─ 4. detailPage — AI 응답 상세 (3유형)
│    │     ├─ 4-A 촬영 후: 위치바 + AI텍스트(배경:촬영이미지) + [▶재생, 공유, 저장]
│    │     ├─ 4-B 음성 후: 위치바 + 🎤쿼리 + AI텍스트 + [🎤추가질문, ▶재생, 공유, 저장]
│    │     └─ 4-C 업로드 후: 4-A와 동일 (배경만 업로드 이미지)
│    │
│    ├─ 5. archivePage — 보관함
│    │     ├─ 상단: ← 리턴 + "보관함"
│    │     ├─ 추천갤러리 카드 최대 3개 (가로 스크롤)
│    │     ├─ 사용자 저장 상세페이지 (무한 스크롤) → guideDetailPage 뷰어
│    │     └─ 하단 버튼 4개: 프로필 / 공유 / 삭제 / 설정
│    │
│    ├─ 6. settingsPage — 사용자 설정
│    │     ├─ AI 면책 안내
│    │     ├─ 사용 방법
│    │     ├─ QR 코드로 전달하기 → QR 모달
│    │     ├─ 고객 지원: FAQ 6가지(아코디언), 버그 신고 & 기능 요청
│    │     ├─ 이용 약관 및 정책 (아코디언)
│    │     ├─ 알림 설정: 푸시 토글
│    │     ├─ 앱 정보: 버전 v1.0.3
│    │     └─ 🔒 관리자 인증 → adminSettingsPage
│    │
│    └─ 7. adminSettingsPage — 관리자 설정
│          ├─ 언어 드롭다운
│          ├─ 드림샷 스튜디오 (AI 이미지&영상, 크레딧)
│          ├─ 테스트용 로그아웃
│          ├─ 관리자 대시보드 열기 → window.open(admin-dashboard.html)
│          ├─ 사용설명서 열기 → window.open(user-guide.html)
│          ├─ 전체 알림 발송
│          ├─ 프롬프트 편집 (이미지/텍스트)
│          ├─ AI 이미지 합성 / AI 영상 제작
│          └─ 기본값 복원 + 저장하기
│
├─── 🔴 완전 독립페이지 (4개) ─── window.open, 별도 세션
│    │
│    ├─ 1. profile.html — 프로필/결제
│    │     ├─ 프로필 사진 + 이름 + 이메일 + 크레딧
│    │     ├─ 내 페이지 탭 (AI 응답 목록, 무한스크롤)
│    │     │   └─ → profileDetailPage (내장 뷰어)
│    │     ├─ 공유페이지 탭 (생성된 공유 목록)
│    │     ├─ 크레딧 내역 탭 (거래 이력)
│    │     ├─ → Google/Kakao OAuth 팝업 (window.open 유지)
│    │     ├─ → Stripe 결제 (window.open 유지)
│    │     └─ → window.open(/s/:id) 공유페이지
│    │
│    ├─ 2. beta.html — 베타 테스터
│    │     ├─ 다국어 UI (한/영)
│    │     ├─ Google OAuth 로그인
│    │     ├─ 베타 혜택 안내 (100 크레딧)
│    │     ├─ 상태: Landing → Processing → Success/Already/Error
│    │     └─ → 외부 링크: Google Group, Play Store
│    │
│    ├─ 3. /s/:id — 추천갤러리(공유)
│    │     ├─ 공개 URL (인증 없음)
│    │     ├─ 서버 렌더링 HTML
│    │     └─ 단독. 파생 없음
│    │
│    └─ 4. admin-dashboard.html — 관리자 대시보드
│          ├─ 탭 7개: 요약/공유페이지/상세페이지/수익/통계/캐시백/AI프롬프트
│          ├─ → adminDetailPage (내장 뷰어)
│          ├─ → window.open(/s/:id) 공유페이지
│          └─ 인증: 비번 0603
│
├─── 🟡 단순 뷰어 페이지 (1개)
│    └─ user-guide.html — 사용설명서 (인포그래픽 이미지 + 복사)
│
├─── 🟢 인앱 모달 (6개) ─── index.html 내 div show/hide
│    ├─ 1. authModal — 인증 (Google/Kakao 로그인)
│    ├─ 2. shareModal — 공유 생성
│    ├─ 3. infographicModal — 인포그래픽 뷰어
│    ├─ 4. videoModal — 동영상 뷰어
│    ├─ 5. QR코드 모달 — QR 복사
│    └─ 6. notificationModal — 알림
│
├─── 🟣 인앱 뷰어 컴포넌트 (1개)
│    └─ guideDetailPage — archivePage 내 상세뷰어 (guideDetailPage.js)
│
└─── 💬 토스트 메시지
      └─ showToast() — 크레딧/인증/저장/삭제/공유/알림 등 전역
```

**P1-4 SPA 전환 대상:**
- profile.html, beta.html, user-guide.html → SPA 오버레이 전환
- /s/:id, admin-dashboard.html → window.open 유지

## 작업 현황 (2026-03-10)

| # | 작업 | 상태 | 비고 |
|---|------|------|------|
| P1-1 | 추천갤러리 버튼 정리 | ✅ 코드완료 | 버튼 통일 + position:fixed 복원 |
| P1-2 | edgeToEdgeEnabled:false | ✅ 코드완료 | APK 빌드 대기 (Replit에서) |
| P1-3 | beta.html OAuth 403 | ✅ P1-4로 흡수 | /?beta=1 리다이렉트 방식 |
| P1-4 | 독립페이지 SPA 전환 | ✅ 코드완료 | iframe 오버레이 방식 |
| P1-5 | guideDetailPage TTS iOS | ✅ 코드완료 | synth.paused 고착 방지 패턴 |
| P1-6 | 공유페이지 변수 충돌 수정 | ✅ 코드완료 | 5개 변수 _tpl 접두어 |
| P1-7 | 런타임 버튼 주입 복원 | ✅ 코드완료 | 2월 1일 로직 + SPA postMessage 호환 |
| — | APK 빌드 | ⏳ 대기 | Replit Linux에서 빌드 필요 |
| — | DB 마이그레이션 | ⏳ 대기 | 배포 후 V1 재생성 필수 (개발/배포 DB 분리) |

---

## 최종 정리 (2026-03-05 검증 완료)

### 인앱 vs 독립 페이지

| 페이지 | 분류 | TTS 방식 | 상태 |
|--------|------|----------|------|
| index.js | 인앱 | 수동 (▶ 버튼) | 이미 완료 |
| guideDetailPage.js | 인앱 | 수동 (▶ 버튼) | 이미 완료 |
| profile.html | 독립 페이지 | 자동재생 | 원래대로 유지 |
| admin-dashboard.html | 독립 페이지 | 자동재생 | 원래대로 유지 |

**결론**: 인앱 2곳만 수동 버튼. 독립 페이지 2곳은 자동재생 유지. **코드 변경 불필요.**

---

## 1. 인앱 TTS 상세 (index.js)

### 1.1 입력 방식 4종류 (모두 동일한 수동 버튼 흐름)

| # | 입력 방식 | 트리거 | 핵심 함수 | API |
|---|-----------|--------|-----------|-----|
| 1 | 촬영 | shootBtn | capturePhoto → processImage | generateDescriptionStream |
| 2 | 이미지 업로드 | uploadInput | handleFileSelect → processImage | generateDescriptionStream |
| 3 | 음성 입력 | micBtn | handleMicButtonClick → processTextQuery | generateTextStream |
| 4 | 음성 추가 질문 | detailMicBtn | handleDetailMicClick → processTextQuery | generateTextStream |

### 1.2 공통 TTS 흐름 (4곳 동일)

```
AI 응답 → queueForSpeech() (큐에만 추가, 자동재생 없음)
       → updateAudioButton('play') (▶ 버튼 표시)
       → 사용자 탭 → onAudioBtnClick() → speakNext() → 재생
```

### 1.3 코드 위치 (processImage / processTextQuery 동일 패턴)

- queueForSpeech: 2591/2602 (processImage), 2817/2828 (processTextQuery)
- updateAudioButton('play'): 2607 (processImage), 2832 (processTextQuery)
- onAudioBtnClick → speakNext: 4060줄

---

## 2. 인앱 TTS 상세 (guideDetailPage.js)

- **위치**: `public/components/guideDetailPage.js` 567~578줄
- **동작**: `_updateAudioButtonIcon(false)` → ▶ 버튼만 표시, 탭 시 `_toggleAudio()` → 재생
- **상태**: 수동 버튼 방식 적용 완료

---

## 3. 독립 페이지 (유지)

- **profile.html**: `_playAudio` 자동재생 — 인앱 아님, 원래대로 유지
- **admin-dashboard.html**: 동일 — 원래대로 유지

---

## 4. 관련 파일 목록

| 구분 | 파일 경로 |
|------|-----------|
| 인앱 (수동) | `public/index.js` |
| 인앱 (수동) | `public/components/guideDetailPage.js` |
| 독립 (자동재생) | `public/profile.html` |
| 독립 (자동재생) | `public/admin-dashboard.html` |
| 수정 불필요 | `shared-template/v2.js`, `share-page.js` |

---

## 5. 용어 설명 (비개발자용)

| 용어 | 설명 |
|------|------|
| TTS | 텍스트를 음성으로 읽어주는 기능 |
| 자동재생 | 화면을 열면 사용자 동작 없이 소리가 나는 것 |
| ▶ 버튼 | 재생 버튼. 탭하면 음성이 재생됨 |
| 인앱 | Expo 앱 WebView 내에서 열리는 화면 |
| 독립 페이지 | window.open으로 별도 탭/창에서 열리는 화면 |

---

---

## 2026-03-08 업데이트

### 작업 원칙 (이후 모든 작업에 적용)
1. 모든 수정 = Android 앱(WebView) 기준
2. 모든 수정 코드에 한국어 주석 (`// ⚠️ 수정금지(승인필요)` + 기능 설명)
3. 모든 문서 = 이 파일에 날짜별 통합
4. 크레딧/결제 = 상업화 수준 완성

### 수정 완료

#### 1. Features footer safe-area padding 복원
- `public/index.html` line 952
- 모바일 노치/홈 인디케이터 영역과 "바로시작하기" 버튼 겹침 해결
- `padding: 12px 24px calc(max(12px, env(safe-area-inset-bottom, 20px)));`

#### 2. WebView OAuth → mainPage(카메라+입력) 직행
- **문제**: WebView에서 OAuth 팝업 차단 → 전체 페이지 이동 → 랜딩 페이지에 갇힘
- **수정 파일**:
  - `server/googleAuth.ts` — OAuth 콜백 else/catch에 localStorage 플래그 추가
  - `server/kakaoAuth.ts` — 동일
  - `public/index.js` — `checkAuthStatusAndCloseModal()`에서 auth_success 감지 시 mainPage 직행

#### 3. Beta 테스터 페이지 복원
- `public/beta.html` — 커밋 b89983f에서 삭제됨 → 복원
- `loginWithGoogle()`에 `sessionStorage.setItem('betaSource', '1')` 추가
- OAuth 후 `/api/beta/play-register` 자동 호출 연동

### 크레딧/결제 시스템 검토 결과
- **Stripe 체크아웃**: 10 EUR = 140 크레딧 (완성)
- **결제 검증**: 중복 방지 완료 (`verify-payment`)
- **캐시백**: 1000크레딧 이상, 관리자 승인 방식 (완성)
- **레퍼럴**: 추천인 20크레딧 자동 지급 (완성)
- **가입 보너스**: 140 크레딧 (프로모션 기간 유지)
- **이중지급 방지**: `grantSignupBonus` idempotent + `grantPromoBonus` 신규유저 스킵
- **프로모션 바이패스** (`return true;`): 사용자 종료 선언 시까지 유지
- **Webhook**: verify-payment로 충분, 보류

### 신규가입 경로 전체 확인
| 경로 | DB 저장 | 140크레딧 | 이중지급 |
|------|---------|-----------|----------|
| Google OAuth | ✅ upsertUser | ✅ grantSignupBonus | ❌ 없음 |
| Kakao OAuth | ✅ upsertUser | ✅ grantSignupBonus | ❌ 없음 |
| /beta → OAuth | ✅ upsertUser | ✅ grantSignupBonus | ❌ 없음 |

---

## 2026-03-08 WebView 종합 리서치 + 로드맵

### 전략

```
Phase 1: 웹 버전 버그 수정 (현재) — 웹에서 완벽 작동 검증
Phase 2: SvelteKit 프론트엔드 전환 (2~3주) — 코드 정리 + 컴포넌트화
Phase 3: Capacitor 앱 컴파일 (1~2주) — Expo WebView 대체
```

### Phase 1: 웹 버전 버그 수정

#### 1-1. 추천갤러리 버튼 정리 (우선)
- **문제**: X 닫기 + ← 리턴 버튼 겹침 (선임 AI 중복 생성)
- **정상 상태**: X 닫기 버튼 1개만 존재 (우상단). ← 리턴 제거, 리다이렉션 금지
- **수정 파일**:
  - `server/standard-template.ts` (484-502) — ← 리턴 버튼 제거
  - `server/index.ts` `injectReferralAndUpdateButton()` (162-177) — ← 런타임 주입 제거
  - `public/index.js` `generateShareHTML()` (1122-1131) — 클라이언트 동일 수정
- **DB 마이그레이션**: admin-dashboard.html 기존 "V1 재생성" 버튼에 연결 (새 버튼 금지)

#### 1-2. 삼성앱 하단 버튼 무반응
- **원인**: `app.json` `edgeToEdgeEnabled: true` → 시스템 내비바가 하단 버튼 위에 겹침
- **수정**: `edgeToEdgeEnabled: false` (APK 재빌드 필요)

#### 1-3. beta.html OAuth 403
- **원인**: WebView `X-Requested-With` 헤더 → Google 차단
- **수정**: `loginWithGoogle()` → `window.location.href = '/?beta=1'` (메인 앱 인증 활용)

#### 1-4. 독립페이지 → SPA 오버레이 전환
- **원인**: `window.open('_blank')` + `window.close()` 모든 WebView에서 작동 불가

| 대상 | 위치 (index.js) | 수정 방향 |
|------|----------------|----------|
| profile.html | line 553, 1947, 1962, 1965 | SPA 오버레이 |
| dream-studio.html | line 4990 | SPA 오버레이 |
| user-guide.html | 별도 링크 | SPA 오버레이 |
| beta.html | 1-3에서 해결 | SPA 오버레이 |
| 공유 URL | line 3361, 3551, 5263 | window.open 유지 |
| YouTube | line 5008 | window.open 유지 |
| OAuth 팝업 | line 5213, 5233 | 보호코드 유지 |

#### 1-5. guideDetailPage TTS iOS Safari 워크어라운드
- **원인**: `_stopAudio()`에 iOS Safari 패턴 누락
- **수정**: `index.js` 패턴 복사 (`synth.cancel → paused → resume → cancel`)
- **파일**: `public/components/guideDetailPage.js` (lines 819-822)

### Phase 2: SvelteKit 프론트엔드 전환 (2~3주)
- 5,000줄 → ~2,000줄 (40~60% 감소), 15-20개 컴포넌트
- Express 백엔드 변경 없음
- SSR OFF → 순수 SPA, WebView 호환 문제 없음
- Svelte Native 사용 금지 (1년+ 미업데이트)

### Phase 3: Capacitor 앱 컴파일 (1~2주)
- Expo WebView → 앱 안에 번들링 (즉시 시작, 오프라인)
- 네이티브 카메라/위치/권한 플러그인
- window.open 문제 근본 해결

### 리서치 근거

**업계 결론**: window.open() + window.close()는 모든 WebView에서 작동 안 함

| 프레임워크 | window.open() | window.close() |
|-----------|--------------|----------------|
| Capacitor | 시스템 브라우저 이탈 | No-op |
| Cordova | InAppBrowser 후킹 | 내부에서 불가 |
| react-native-webview | onOpenWindow 감지만 | 미지원 |

**GitHub 미해결 이슈**: react-native-webview #1868, #1699, #2400 / Capacitor #1327, #1414, #2660

**실제 앱 사례 (10건)**:
| 앱 | 접근법 |
|----|--------|
| Basecamp/HEY | Hotwire Native |
| Shopify (600+ 스크린) | postMessage 브릿지 |
| Notion | WebView → 네이티브 전환 |
| Discord | 순수 React Native |
| Capacitor/Ionic 전체 | SPA 전용 |

**SvelteKit + Capacitor 사례**: iOS/Android 출시 앱 다수 확인 (Ionic 공식 100% SPA)

**공유페이지 HTML 저장 구조**:
- `sharedHtmlPages.htmlContent`에 전체 HTML 저장 (DB)
- 서빙 시 `injectReferralAndUpdateButton()`이 런타임 버튼 주입
- featured 페이지: 템플릿 + 런타임 주입으로 버튼 3개 겹침 → X 1개만 유지해야 함

---

## 2026-03-09 업데이트 (Claude Opus 4.6)

### 3가지 규칙 (모든 독립페이지 적용)
1. 독립페이지 X 닫기 → 리다이렉션 절대 없음 (postMessage로 오버레이 닫기)
2. 상세뷰 ← 리턴 → 부모페이지로 복귀만 (닫기 아님)
3. 모든 독립페이지 → SPA 오버레이 구조

### 완료된 작업

| # | 작업 | 파일 | 내용 |
|---|------|------|------|
| TODO 1 | 공유페이지 템플릿 복원 + SPA | standard-template.ts | closeWindowBtn 삭제, 갤러리 버튼 복원 + iframe postMessage |
| TODO 2 | 공유페이지 열기 SPA 전환 | index.js:3574 | handleFeaturedClick window.open → openPageOverlay |
| TODO 3 | beta.html 인증 흐름 | beta.html, index.js | postMessage → authModal → authComplete → phase 2 |
| TODO 4 | admin-dashboard SPA | index.html, admin-dashboard.html | openPageOverlay + postMessage 닫기 |
| TODO 5 | profile.html 공유페이지 | profile.html:783 | parent.openPageOverlay |

### 규칙 점검 결과 (수정 후)

**규칙 1: X 닫기 (리다이렉션 없음)**
| 페이지 | 상태 |
|--------|------|
| 공유페이지 /s/:id | ✅ iframe postMessage / else window.close() |
| profile.html | ✅ postMessage (이전 완료) |
| user-guide.html | ✅ postMessage (이전 완료) |
| beta.html | ✅ X 불필요 (자동 흐름) |
| admin-dashboard.html | ✅ iframe postMessage |

**규칙 2: 상세뷰 ← 리턴**
| 상세뷰 | 상태 |
|--------|------|
| 공유페이지 detail-back | ✅ closeWindowBtn 제거로 정상화 |
| guideDetailPage | ✅ 기존 정상 |
| profileDetailPage | ✅ 기존 정상 |
| adminDetailPage | ✅ 기존 정상 |

**규칙 3: 오버레이 구조**
| 페이지 | 상태 |
|--------|------|
| profile.html | ✅ openPageOverlay |
| user-guide.html | ✅ openPageOverlay |
| beta.html | ✅ openPageOverlay + postMessage OAuth |
| 공유페이지 /s/:id | ✅ openPageOverlay |
| admin-dashboard.html | ✅ openPageOverlay |

### 배포 후 필수 작업
- 관리자 대시보드 → "V1 재생성" 클릭 → DB 구버전 공유페이지 갱신

### P1-4 보완: overlay X 버튼 겹침 해결 ✅ (2026-03-09)
- **문제**: pageOverlay의 `closePageOverlayBtn` (iframe 바깥, top-right)이 독립페이지 자체 버튼을 덮어씀
- **원인**: SPA 오버레이 구현 시 불필요한 X 버튼을 iframe 위에 임의 추가한 것
- **수정**: `public/index.html:882-886` — `closePageOverlayBtn` 버튼 HTML 삭제 (1곳)
- **영향**: 5개 독립페이지(공유페이지 추천/일반, admin-dashboard, profile, user-guide) 자체 버튼 동시 복원
- **유지**: `closePageOverlay()` 함수 (postMessage 수신에서 사용)
- **참고**: beta.html은 자체 닫기 버튼 미보유 → TODO 6에서 처리

### 미완료 (TODO 6 예정)
- → 2026-03-10 섹션으로 이관

---

## 2026-03-10 업데이트

### 동료(kangwwokkim) 작업

#### 1. 공유페이지 버튼 통일 — isFeatured 분기 제거
- **커밋**: `2a05afa`
- **수정 파일**: `server/standard-template.ts`, `public/admin-dashboard.html`
- **내용**: isFeatured 분기 삭제, 단일 gallery-close X 버튼 통일

#### 2. gallery-close position 시행착오 + 최종 복원
- **커밋**: `5456080` → `6be8f1b` (최종)
- **시행착오**: `position:fixed → absolute` 변경 시도 → 결국 `position:fixed` 복원
- **최종 수정**: JS토글 2줄 제거 (부모 `display:none`으로 자동 숨김이 CSS 표준 동작)
- **수정 파일**: `server/standard-template.ts`

#### 3. 공유페이지 마이그레이션
- **커밋**: `9a86c1b`
- **수정 파일**: `public/shared/*.html` 4개 (3KnP-HAJ, Alis-whv, YHRpPQbt, hsy_93G2)
- **내용**: 버튼 구조 변경 반영 (정적 HTML 파일 업데이트)

### P1-6: 공유페이지 인라인 스크립트 변수 충돌 수정 ✅

- **문제**: 공유페이지 접속 시 `Uncaught SyntaxError: Identifier 'voiceConfigsCache' has already been declared` 발생 → share-page.js 전체 실행 실패 → TTS/언어감지 기능 작동 불가
- **원인**: `standard-template.ts` 인라인 `<script>`와 `share-page.js`가 동일 전역 스코프에서 `let/const` 변수를 중복 선언
- **수정 파일**: `server/standard-template.ts` (인라인 스크립트 블록 2곳)
- **수정 방법**: 충돌 변수에 `_tpl` 접두어 추가

| 원본 (충돌) | 수정 후 | share-page.js (변경 없음) |
|------------|---------|--------------------------|
| `let voiceConfigsCache` | `let _tplVoiceConfigsCache` | `let voiceConfigsCache` |
| `let voiceConfigsLoading` | `let _tplVoiceConfigsLoading` | `let voiceConfigsLoading` |
| `const DEFAULT_VOICE_PRIORITIES` | `const _TPL_DEFAULT_VOICE_PRIORITIES` | `const DEFAULT_VOICE_PRIORITIES` |
| `let retranslationPending` | `let _tplRetranslationPending` | `let retranslationPending` |
| `let voices` | `let _tplVoices` | `var voices` |

### 배포 후 필수 작업
- 관리자 대시보드 → "V1 재생성" 클릭 → 기존 공유페이지에 P1-1(버튼 통일) + P1-6(변수 충돌 수정) 일괄 반영
- `public/shared/*.html` 4개 파일도 재생성 시 자동 덮어쓰기

### P1-7: 런타임 버튼 주입 복원 — 2월 1일 안정 로직 + SPA 호환 ✅ (2026-03-10)

- **문제**: `9a7241e` 커밋에서 server/index.ts 런타임 버튼 주입 로직이 반대로 변경됨
  - 변경 전(2월 1일 안정): closeWindowBtn(X) 제거 → shareReturnBtn(←) 추가 + detail-back 가드
  - 변경 후(버그): shareReturnBtn(←) 제거 → closeWindowBtn(X) 추가 + 가드 없음
- **증상**: body 직속 closeWindowBtn(X, z-index:10000)이 `.ui-layer`(z-index:10) 안의 detail-back(←) 위에 겹침 → 상세뷰에서 ← 리턴 버튼 클릭 불가
- **수정 파일**: `server/index.ts` (line 165~181)
- **수정 내용**:
  1. closeWindowBtn(X) 런타임 제거 regex 추가
  2. shareReturnBtn(←) SPA 호환 주입: `postMessage('closeOverlay')` (iframe) / `window.close()` (독립)
  3. 가드: `gallery-close` 보유 시 스킵 (V1 페이지는 자체 닫기 버튼 사용)
- **SPA 호환성 분석**:
  - V1 페이지: gallery-close(postMessage) + detail-back(←) → 가드 작동, shareReturnBtn 미주입 → 정상
  - 구버전 페이지: gallery-close 없음 → shareReturnBtn 주입 → SPA iframe에서 postMessage로 닫기 → 정상
- **검증**: curl V1 페이지 — closeWindowBtn:0, gallery-close:1, detail-back:2, shareReturnBtn:0

### 배포 후 필수 작업 (업데이트)
1. **배포** → production 서버에 P1-7 런타임 코드 적용
2. **V1 마이그레이션** → 관리자 대시보드 "V1 재생성" 클릭 또는 API 호출
   - 개발 DB: 완료 (39개 전체)
   - 배포 DB: 미완료 — 구버전 HTML 저장 상태
   - P1-1(버튼 통일) + P1-6(변수 충돌) + P1-7(런타임 버튼) 모두 반영됨
3. `public/shared/*.html` 4개 파일도 재생성 시 자동 덮어쓰기

### 미완료 (TODO 6 예정)
- beta.html 단순화 + 이메일 자동 발송
- 인증 후 1시간 뒤 관리자 Gmail(dbstour1@gmail.com)에서 테스터 링크 자동 발송
- 필요: Gmail App Password (Replit Secrets에 이미 존재 가능, 2026-02-25 사용 이력)
- beta.html Google Group 버튼 삭제 + success 화면 "1시간 후 메일 발송 안내"로 변경
- beta.html X 닫기 버튼 + postMessage closeOverlay 추가

---

## 세션 3 적용 내역 (2026-03-10)

### 완료
| 태스크 | 파일 | 내용 |
|--------|------|------|
| T001 | profile.html, index.js | iframe 리다이렉트 일괄 수정 (replace_all) + openPageOverlay base tag 주입 |
| T002 | index.js | pendingShareUrl window.open → openPageOverlay 4곳 |
| T003 | App.js | injectedJavaScript(safe-area CSS), PermissionsAndroid, mediaCapturePermissionGrantType, sharedCookiesEnabled |
| T004 | app.json | android.permissions: CAMERA, RECORD_AUDIO, ACCESS_FINE_LOCATION |
| T005 | index.html, profile.html, standard-template.ts | viewport-fit=cover 4곳 + safe-area CSS 순서 수정 |
| T006 | dist/ | 빌드 완료, 배포 제안 |
| T008 | 전체 | 보호 주석 + replit.md + docs 업데이트 |

### 미완료
| 태스크 | 사유 |
|--------|------|
| T006 V1 재생성 | 관리자 인증 필요 — 배포 후 관리자 대시보드에서 실행 |
| T007 EAS 빌드 | 기존 preview 빌드 진행 중, 완료 후 App.js/app.json 반영 새 빌드 필요 (versionCode 5) |

---

*최종 업데이트: 2026-03-10*
