# 2026-03-20 테스트 체크리스트

## 수정 내역
| # | 수정 | 파일 | 내용 |
|---|------|------|------|
| Fix 1 | 프로필 로그아웃 버튼 | profile.html | guestView 안 → profileContent 하위 독립 위치로 이동 (항상 표시) |
| Fix 2 | OAuth 모바일 직접 이동 | index.js:5588 | `isNativeApp` → `isNativeApp \|\| isMobile()` (모바일 팝업 차단 우회) |
| Fix 3 | 딥링크 제거 | googleAuth.ts, appleAuth.ts | 딥링크+3초대기 삭제 → kakaoAuth.ts와 동일 패턴 (`location.replace('/')`) |

---

## A. 프로필 로그아웃 (Fix 1) — Replit Publish 후 테스트
| # | 테스트 항목 | O/X | 기대효과 |
|---|-----------|-----|---------|
| A-1 | 프로필 → 로그아웃 버튼 **보임** (인증 상태) | | 이전: guestView 안에 숨겨져 인증자에게 안 보임 → 이후: profileContent 하위, 항상 표시 |
| A-2 | 로그아웃 클릭 → **랜딩페이지** 이동 (보관함 아님) | | 이전: /archive로 이동 → 이후: / 이동 + 모든 인증 캐시 삭제 |
| A-3 | 로그아웃 후 재접속 → "바로 시작하기" → **인증모달** 표시 | | cachedUser 삭제 → checkUserAuth() null 반환 → 인증모달 차단 |

## B. Google OAuth 모바일 (Fix 2 + Fix 3) — Replit Publish 후 모바일 브라우저 테스트
| # | 테스트 항목 | O/X | 기대효과 |
|---|-----------|-----|---------|
| B-1 | 모바일 브라우저 → Google 로그인 → **팝업 없이 직접 이동** | | 이전: window.open 팝업 차단 → 먹통 → 이후: isMobile() → location.href 직접 이동 |
| B-2 | Google 인증 완료 → **깜빡임 없이** 메인페이지 진입 | | 이전: 딥링크 시도 → 에러/3초 대기 → 이후: 딥링크 제거 → 바로 / 이동 |
| B-3 | PC 브라우저 → Google 로그인 → **기존 팝업 방식 유지** | | isMobile()=false → 기존 window.open 팝업 유지 (변경 없음) |

## C. Apple OAuth (Fix 2 + Fix 3) — Replit Publish 후 iOS 브라우저 테스트
| # | 테스트 항목 | O/X | 기대효과 |
|---|-----------|-----|---------|
| C-1 | iOS Safari → Apple 로그인 버튼 **보임** | | detectPlatform()='ios' → display:flex |
| C-2 | Apple 로그인 → 시트 → 인증 완료 → **메인페이지 진입** | | 이전: 딥링크 깜빡임 + 3초 대기 → 이후: 즉시 / 이동 |
| C-3 | Kakao 로그인 → 정상 작동 유지 | | kakaoAuth.ts 수정 없음 — 기존 동작 보존 확인 |

## D. OAuth WebView 앱 내 (Fix 2) — **EAS 빌드 후** 앱 테스트
| # | 테스트 항목 | O/X | 기대효과 |
|---|-----------|-----|---------|
| D-1 | 앱 → Google 로그인 → WebView 내 직접 이동 → 인증 완료 | | isNativeApp=true → location.href (팝업 차단 회피) |
| D-2 | 앱 → Kakao 로그인 → 정상 동작 | | 기존과 동일 |
| D-3 | 앱 → Apple 로그인 (iOS) → 정상 동작 | | 딥링크 제거 → WebView 내 postMessage 분기 |

## E. 네이티브 음성인식 — **EAS 빌드 후** 앱 테스트
| # | 테스트 항목 | O/X | 기대효과 |
|---|-----------|-----|---------|
| E-1 | 마이크 버튼 → 네이티브 음성인식 시작 (권한 요청) | | expo-speech-recognition → 네이티브 브릿지 |
| E-2 | 말하기 → 텍스트 변환 → AI 질문 전달 | | speechResult → processTextQuery() |
| E-3 | 웹 브라우저 → 기존 Web Speech API 동작 | | 웹: SpeechRecognition 사용 (변경 없음) |

## F. Apple 로그인 버튼 — iOS 테스트
| # | 테스트 항목 | O/X | 기대효과 |
|---|-----------|-----|---------|
| F-1 | iOS 앱/브라우저: Apple 로그인 버튼 **보임** | | detectPlatform()='ios' → display:flex |
| F-2 | Android: Apple 로그인 버튼 **안 보임** | | detectPlatform()≠'ios' → 기본 hidden |
